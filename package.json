{
  "type": "module",
  "name": "mcp-server-test",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.ts --bundle --outfile=server.js --format=esm --platform=node"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "@types/node": "^22.14.1",
    "esbuild": "^0.25.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
